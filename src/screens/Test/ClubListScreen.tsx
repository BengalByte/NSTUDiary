import {View, Text} from 'react-native';
import React from 'react';
import {ClubListScreenProps} from 'navigation/types';

const ClubListScreen = ({route, navigation}: ClubListScreenProps) => {
  return (
    <View>
      <Text>ClubListScreen</Text>
    </View>
  );
};

export default ClubListScreen;
